import styled from "styled-components";
import React, { useState } from 'react';

interface ContainerProps{
    bgColor : string;
    borderColor : string;
}

const Container = styled.div<ContainerProps>`
    width:200px;
    height:200px;
    background-color:  ${props => props.bgColor};
    border-radius: 100px;
    border : 1px solid ${props => props.borderColor};
`;

interface CircleProps {
    bgColor : string;
    borderColor? : string;
    text? :string;
}

function Circle({bgColor, borderColor, text = "default text"} : CircleProps) {
    const [value, setVlaue] = useState<number|string>(0);
    setVlaue(2)
    return <Container bgColor={bgColor} borderColor={borderColor ?? "white"}>{text}</Container>
}

// interface PlayerShpae{
//     name : string,
//     age : number
// }

// const sayHello = (playerObj:PlayerShpae) => `Hello ${playerObj.name} You are ${playerObj.age} years old`


// sayHello({name : "nico", age : 29})
// sayHello({name : "hi", age : 12, hello:1})

export default Circle